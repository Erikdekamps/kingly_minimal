$schema: https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json
name: 'Card'
status: stable
description: 'A versatile component for displaying content in a card-like format. Composes other SDCs like image, text, and button.'

props:
  type: object
  properties:
    variant:
      type: string
      title: Variant
      description: Controls the layout of the card.
      default: 'vertical'
      enum:
        - 'vertical'
        - 'horizontal'
    image:
      type: object
      title: 'Image'
      description: 'An object of props for the `kingly_minimal:image` component (e.g., src, alt).'
    title:
      type: string
      title: 'Title'
      description: 'The main heading for the card.'
    body:
      type: [string, object] # Can be a string or render array
      title: 'Body'
      description: 'The main content for the card, passed to the `text` component for consistent prose styling.'
    link:
      type: object
      title: Link
      description: An object containing the link URL and text. If provided, the entire card becomes a clickable link.
      properties:
        url:
          type: string
          title: URL
        text:
          type: string
          title: Text
    attributes:
      type: object
      title: 'Attributes'
      description: 'A Drupal attributes object for the root element.'

# This component is fully prop-driven, so it does not define slots.
slots: {}

library:
  css:
    component:
      card.css: {}
