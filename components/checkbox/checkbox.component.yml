$schema: https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json
name: 'Checkbox'
status: stable
description: 'Renders a standard HTML checkbox input element, typically used via a bridge template from the Form API.'

props:
  type: object
  properties:
    attributes:
      type: object
      title: 'Attributes'
      description: 'A Drupal attributes object for the `<input type="checkbox">` element. Contains all necessary HTML attributes.'
