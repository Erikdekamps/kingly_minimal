{#
/**
 * @file
 * Component template for the footer company info section.
 *
 * This component embeds the `footer-widget` component, delegating the wrapper
 * and title markup to it. It places its own content (the description) into
 * the `content` block of the widget.
 *
 * @see kingly_minimal/components/footer-company-info/footer-company-info.component.yml
 *
 * @props
 * - company_name: (String) The name of the company.
 * - description: (String|Object) A short description for the company.
 * - attributes: (Object) A Drupal attributes object for the wrapper.
 */
#}
{% embed 'kingly_minimal:footer-widget' with {
  title: company_name,
  attributes: attributes.addClass('footer-company-info')
} %}
  {% block content %}
    {% if description %}
      {#
      Delegate rendering of the description to the Text component.
      This ensures that if the description contains any simple HTML,
      it will be styled correctly by the .prose class.
      #}
      {{ include('kingly_minimal:text', {
        content: description,
        attributes: create_attribute().addClass('footer-company-info__description'),
      }, with_context=false) }}
    {% endif %}
  {% endblock %}
{% endembed %}
