$schema: https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json
name: 'Footer'
status: experimental
description: 'Renders the main site footer with customizable sections and columns.'

# Declare empty props.
props:
  type: object
  properties: {}
