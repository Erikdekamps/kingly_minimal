$schema: https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json
name: 'Local Tasks'
status: experimental
description: 'Renders the primary and secondary local task tabs (e.g., View, Edit, Revisions).'

# Declare empty props.
props:
  type: object
  properties: {}
