$schema: https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json
name: 'Media: Audio'
status: stable
description: 'Renders an audio media entity with a contextual wrapper.'

props:
  type: object
  properties:
    media_entity:
      type: object
      title: Media Entity
      description: 'The full audio media entity object, passed from the dispatcher.'
    content:
      type: [ string, object ]
      title: Content
      description: 'The rendered HTML5 audio player.'

  required:
    - media_entity
    - content
