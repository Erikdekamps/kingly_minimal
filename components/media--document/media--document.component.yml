$schema: https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json
name: 'Media: Document'
status: stable
description: 'Renders a document media entity with a contextual wrapper.'

props:
  type: object
  properties:
    media_entity:
      type: object
      title: Media Entity
      description: 'The full document media entity object.'
      required: true
    attributes:
      type: object
      title: Attributes
      description: HTML attributes for the wrapper element.

slots:
  content:
    title: Content
    required: true
    description: 'The rendered document link.'

library:
  css:
    component:
      media--document.css: {}
