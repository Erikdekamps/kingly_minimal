{#
/**
 * @file
 * Component template for an SVG image media item.
 * @see kingly_minimal/components/media--svg-image/media--svg-image.component.yml
 * @props
 * - media_entity: (Object) The full SVG image media entity object.
 * - content: (Object) The rendered SVG image element.
 * - attributes: (Object) A Drupal attributes object for the wrapper element.
 */
#}
<div{{ attributes.addClass('media--svg-image') }}>
  {{- content -}}
</div>
