{#
/**
 * @file
 * Component template for an SVG image media item.
 *
 * @see kingly_minimal/components/media--svg-image/media--svg-image.component.yml
 *
 * @props
 * - media_entity: The full SVG image media entity object.
 * - attributes: A Drupal attributes object for the wrapper element.
 *
 * @slots
 * - content: The rendered SVG image element.
 */
#}
<div{{ attributes.addClass('media--svg-image') }}>
  {% block content %}{% endblock %}
</div>
