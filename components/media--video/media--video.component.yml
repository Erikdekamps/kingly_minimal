$schema: https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json
name: 'Media: Video'
status: stable
description: 'Renders a locally hosted video media entity with a contextual wrapper.'

props:
  type: object
  properties:
    media_entity:
      type: object
      title: Media Entity
      description: 'The full local video media entity object.'
      required: true
    content:
      type: [string, object]
      title: Content
      required: true
      description: 'The rendered HTML5 video player.'
    attributes:
      type: object
      title: Attributes
      description: HTML attributes for the wrapper element.

library:
  css:
    component:
      media--video.css: {}
