$schema: https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json
name: 'Media (Dispatcher)'
status: stable
description: 'A dispatcher component that inspects a media entity and renders the appropriate sub-component (e.g., media--image, media--video).'

props:
  type: object
  properties:
    media_entity:
      type: object
      title: Media Entity
      description: 'The full media entity object. Used to determine the media type for dispatching.'

  required:
    - media_entity

slots:
  content:
    title: Content
    description: 'The rendered media entity (e.g., an `<img>` or `<iframe>`) to be passed to the sub-component.'
