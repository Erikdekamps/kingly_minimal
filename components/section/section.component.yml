$schema: https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json
name: Section
status: stable
description: A generic container component, typically used to wrap content from Layout Paragraphs to ensure consistent spacing and containment.

props:
  type: object
  properties:
    attributes:
      type: object
      title: Attributes
      description: HTML attributes for the wrapper element.

slots:
  content:
    title: Content
    required: true
    description: The main content of the section.

library:
  css:
    component:
      section.css: {}
