{#
/**
 * @file
 * Component template for a select dropdown.
 *
 * This component simply renders a <select> tag with its options.
 * It is intended to be used within a `form-element` component.
 *
 * @see kingly_minimal/components/select/select.component.yml
 *
 * @props
 * - options: The pre-rendered string of <option> tags.
 * - attributes: A Drupal attributes object for the <select> element.
 */
#}
<select{{ attributes }}>
  {{- options|raw -}}
</select>
