{#
/**
 * @file
 * Component template for a select dropdown.
 *
 * @see kingly_minimal/components/select/select.component.yml
 *
 * @props
 * - options: (String) The pre-rendered string of <option> tags.
 * - attributes: (Object) A Drupal attributes object for the <select> element.
 */
#}
<select{{ attributes }}>
  {{- options|raw -}}
</select>
