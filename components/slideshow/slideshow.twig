{#
/**
 * @file
 * Component template for the slideshow.
 *
 * This template provides the HTML structure required by Splide.js. The custom
 * header is now a sibling to the .splide container for a cleaner DOM structure,
 * and the JS is responsible for moving the generated arrows into it.
 *
 * @see kingly_minimal/components/slideshow/slideshow.component.yml
 *
 * @props
 * - uuid: A unique ID for this slideshow instance.
 * - title: The main heading for the slideshow.
 * - columns_mobile: The number of slides to show per page on mobile.
 * - columns_tablet: The number of slides to show per page on tablet.
 * - columns_desktop: The number of slides to show per page on desktop.
 * - attributes: A Drupal attributes object for the root wrapper element.
 *
 * @slots
 * - content: The slot for the <li> slide items.
 */
#}
<div{{ attributes.addClass('slideshow') }}>
  <div class="slideshow__header">
    {% if title %}
      <h2 class="slideshow__title">{{ title }}</h2>
    {% endif %}
    {# This empty area is a placeholder. Splide's generated arrows will be
       programmatically appended here by our JavaScript. #}
  </div>

  {#
  This is the main Splide container. It is now a sibling to the header.
  - A unique ID allows the JS to target this specific instance.
  - `role="region"` and `aria-label` make it a distinct landmark for screen readers.
  - `data-*` attributes hold our responsive settings for the JS to consume.
  #}
  <div
    class="splide"
    id="splide-{{ uuid }}"
    role="region"
    aria-label="{{ title|default('Slideshow')|t }}"
    data-columns-mobile="{{ columns_mobile }}"
    data-columns-tablet="{{ columns_tablet }}"
    data-columns-desktop="{{ columns_desktop }}"
  >
    <div class="splide__track">
      <ul class="splide__list">
        {# The individual slide <li> elements will be rendered here from the content slot. #}
        {% block content %}{% endblock %}
      </ul>
    </div>
  </div>
</div>
