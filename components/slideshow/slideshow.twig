{#
/**
 * @file
 * Component template for the slideshow.
 *
 * This template provides the HTML structure required by Splide.js. It now
 * uses the `heading` component to render its title.
 *
 * @see kingly_minimal/components/slideshow/slideshow.component.yml
 *
 * @props
 * - uuid: (String) A unique ID for this slideshow instance.
 * - title: (String) The main heading for the slideshow.
 * - columns_mobile: (String|Number) The number of slides to show per page on mobile.
 * - columns_tablet: (String|Number) The number of slides to show per page on tablet.
 * - columns_desktop: (String|Number) The number of slides to show per page on desktop.
 * - attributes: (Object) A Drupal attributes object for the root wrapper element.
 *
 * @slots
 * - content: The slot for the <li> slide items.
 */
#}
<div{{ attributes.addClass('slideshow') }}>
  <div class="slideshow__header">
    {% if title %}
      {# Delegate rendering to the canonical `heading` component. #}
      {{ include('kingly_minimal:heading', {
        text: title,
        level: 'h2',
        attributes: create_attribute().addClass('slideshow__title')
      }, with_context = false) }}
    {% endif %}
    {# Placeholder for Splide's generated arrows. #}
  </div>

  {#
  This is the main Splide container.
  - `role="region"` and `aria-label` make it a distinct landmark for screen readers.
  - `data-*` attributes hold our responsive settings for the JS to consume.
  #}
  <div
    class="splide"
    id="splide-{{ uuid }}"
    role="region"
    aria-label="{{ title|default('Slideshow')|t }}"
    data-columns-mobile="{{ columns_mobile }}"
    data-columns-tablet="{{ columns_tablet }}"
    data-columns-desktop="{{ columns_desktop }}"
  >
    <div class="splide__track">
      <ul class="splide__list">
        {% block content %}{% endblock %}
      </ul>
    </div>
  </div>
</div>
