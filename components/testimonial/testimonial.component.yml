$schema: https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json
name: 'Testimonial'
status: experimental
description: 'Renders a testimonial as a card. Composes the `heading` component for the author''s name.'

# Declare empty props.
props:
  type: object
  properties: {}
