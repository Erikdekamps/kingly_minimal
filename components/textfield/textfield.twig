{#
/**
 * @file
 * Component template for a text input field.
 *
 * This component simply renders an <input> tag with the provided attributes.
 * It is intended to be used within a `form-element` component, which provides
 * the label and wrapper.
 *
 * @see kingly_minimal/components/textfield/textfield.component.yml
 *
 * @props
 * - attributes: A Drupal attributes object for the <input> element.
 */
#}
<input{{ attributes }} />
