{#
/**
 * @file
 * Theme override for the system branding block.
 *
 * This template acts as a bridge to the 'branding' Single Directory Component.
 * It passes the necessary variables from the block's context to the component.
 *
 * @see kingly_minimal/components/branding/branding.twig
 */
#}
{{ include('kingly_minimal:branding', {
  is_front: is_front,
  front_page: front_page,
  site_name: site_name,
  site_slogan: site_slogan,
  attributes: attributes,
}, with_context = false) }}
