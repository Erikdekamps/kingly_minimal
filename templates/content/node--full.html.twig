{#
/**
 * @file
 * Theme override for a node in the 'full' view mode.
 *
 * This template acts as a bridge to the 'node-full' Single Directory Component.
 * It maps the variables available in this context to the props expected by the
 * component. The SDC now safely handles renderable variables.
 *
 * @see template_preprocess_node()
 * @see kingly_minimal/components/node-full/node-full.twig
 */
#}
{%
  set classes = [
  'node',
  'node--type-' ~ node.bundle|clean_class,
  node.isPromoted() ? 'node--promoted',
  node.isSticky() ? 'node--sticky',
  not node.isPublished() ? 'node--unpublished',
  view_mode ? 'node--view-mode-' ~ view_mode|clean_class,
]
%}
{#
This bridge passes the raw `author_name` variable to the SDC.
The SDC is now responsible for handling it safely within a {% trans %} block,
preventing any fatal errors.
#}
{{ include('kingly_minimal:node-full', {
  label: label,
  page: page,
  url: url,
  display_submitted: display_submitted,
  author_picture: author_picture,
  author_name: author_name,
  date: date,
  metadata: metadata,
  content: content,
  content_attributes: content_attributes,
  title_prefix: title_prefix,
  title_suffix: title_suffix,
  attributes: attributes.addClass(classes),
}, with_context = false) }}
