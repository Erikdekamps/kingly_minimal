{#
/**
 * @file
 * Theme override for a single search result.
 *
 * Available variables:
 * - result: The search result item.
 *   - link: A link to the search result.
 *   - title: The title of the search result.
 *   - snippet: A snippet of the search result.
 *   - info: Additional information about the search result.
 * - plugin_id: The id of the search plugin that produced the result.
 * - title_prefix: Additional output populated by modules, intended to be
 *   displayed in front of the main title tag that appears in the template.
 * - title_suffix: Additional output populated by modules, intended to be
 *   displayed after the main title tag that appears in the template.
 * - url: The URL of the search result.
 *
 * @see template_preprocess_search_result()
 */
#}
<div class="search-result">
  {{ title_prefix }}
  <h3 class="search-result__title">
    <a href="{{ url }}">{{ title }}</a>
  </h3>
  {{ title_suffix }}
  <div class="search-result__info">{{ info }}</div>
  {% if snippet %}
    <p{{ content_attributes.addClass('search-result__snippet') }}>{{ snippet }}</p>
  {% endif %}
</div>
