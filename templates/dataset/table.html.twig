{#
/**
 * @file
 * Theme override for a 'table' element.
 *
 * This template now acts as a bridge to the 'table' Single Directory Component.
 * It passes the necessary variables from the theme system to the component,
 * which now handles all markup and styling.
 *
 * @see kingly_minimal/components/table/table.twig
 */
#}
{{ include('kingly_minimal:table', {
  caption: caption,
  header: header,
  footer: footer,
  rows: rows,
  colgroups: colgroups,
  empty: empty,
  table_attributes: attributes,
}, with_context = false) }}
