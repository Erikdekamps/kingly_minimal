{#
/**
 * @file
 * Theme override for an 'input' element of type 'radio'.
 *
 * This template acts as a bridge to the 'radio' Single Directory Component.
 * It passes the attributes from the theme system to the component.
 *
 * @see kingly_minimal/components/radio/radio.twig
 */
#}
{{ include('kingly_minimal:radio', {
  attributes: attributes,
}, with_context = false) }}
