{#
/**
 * @file
 * Theme override for a text input.
 *
 * This template now acts as a bridge to the 'textfield' Single Directory
 * Component. It passes the attributes from the theme system to the component.
 *
 * @see kingly_minimal/components/textfield/textfield.twig
 */
#}
{{ include('kingly_minimal:textfield', {
  attributes: attributes,
}, with_context = false) }}
