{#
/**
 * @file
 * Theme override for a select element.
 *
 * This template now acts as a bridge to the 'select' Single Directory
 * Component. It passes the attributes and options from the theme system to
 * the component.
 *
 * @see kingly_minimal/components/select/select.twig
 */
#}
{{ include('kingly_minimal:select', {
  attributes: attributes,
  options: options,
}, with_context = false) }}
