{#
/**
 * @file
 * Theme override for status messages.
 *
 * This template acts as a bridge to the 'status-messages' Single Directory
 * Component. It passes the necessary variables from the theme system to the
 * component, which now handles all markup and styling.
 *
 * @see kingly_minimal/components/status-messages/status-messages.twig
 */
#}
{{ include('kingly_minimal:status-messages', {
  message_list: message_list,
  status_headings: status_headings,
}, with_context = false) }}
