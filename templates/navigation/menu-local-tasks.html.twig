{#
/**
 * @file
 * Theme override to display primary and secondary local tasks.
 *
 * This template has been simplified to act as a bridge to the 'local-tasks'
 * Single Directory Component. It passes the raw 'primary' and 'secondary'
 * render arrays to the component, which handles all markup and styling.
 *
 * @see kingly_minimal/components/local-tasks/local-tasks.twig
 */
#}
{{ include('kingly_minimal:local-tasks', {
  primary: primary,
  secondary: secondary,
}, with_context = false) }}
