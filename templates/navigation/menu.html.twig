{#
/**
 * @file
 * Theme override to display a menu.
 *
 * This template now acts as a bridge to the 'menu' Single Directory Component.
 * It passes the necessary variables from the theme system to the component,
 * which now handles all markup, styling, and recursive rendering logic.
 *
 * @see kingly_minimal/components/menu/menu.twig
 */
#}
{{ include('kingly_minimal:menu', {
  items: items,
  attributes: attributes,
}, with_context = false) }}
