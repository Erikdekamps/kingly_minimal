{#
/**
 * @file
 * Theme override for the Accordion Item paragraph.
 *
 * This template acts as a bridge, mapping the paragraph's raw field values
 * directly to the props of the 'kingly_minimal:accordion-item' SDC.
 * This bypasses all intermediate field templates for the cleanest possible markup.
 *
 * @see kingly_minimal/components/accordion-item/accordion-item.twig
 */
#}
{#
- Access the raw value from the paragraph entity for clean data.
- Apply the `|raw` filter to the content field to render its HTML.
- The heading is a plain text string, so it doesn't need the raw filter.
#}
{{ include('kingly_minimal:accordion-item', {
  attributes: attributes,
  heading: paragraph.field_kingly_accordion_heading.value,
  content: paragraph.field_kingly_accordion_content.value|raw,
}, with_context = false) }}
