{#
/**
 * @file
 * Theme override for the Section paragraph type.
 *
 * This template acts as a bridge, mapping the entire rendered content of the
 * paragraph (which typically comes from Layout Paragraphs) into the 'content'
 * slot of the 'kingly_minimal:section' Single Directory Component.
 *
 * Available variables:
 * - paragraph: The paragraph entity.
 * - content: All paragraph items. Preprocessed.
 * - attributes: HTML attributes for the wrapper.
 * - All DDC variables are available.
 */
#}
{% embed 'kingly_minimal:section' with {
  attributes: attributes,
} %}

  {% block content %}
    {#
    The 'content' variable here contains the entire rendered layout
    from the Layout Paragraphs module, including the layout plugin's wrapper
    and all child paragraphs within their respective regions. We pass this
    directly into our component.
    #}
    {{ content }}
  {% endblock %}

{% endembed %}
