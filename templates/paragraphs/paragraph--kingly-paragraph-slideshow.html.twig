{#
/**
 * @file
 * Theme override for the Slideshow container paragraph.
 *
 * This template acts as a bridge, mapping the paragraph's field values to the
 * props of the 'kingly_minimal:slideshow' SDC. The rendered child paragraphs
 * (the slides) are passed into the component's 'content' slot.
 *
 * @see kingly_minimal/components/slideshow/slideshow.twig
 */
#}
{% embed 'kingly_minimal:slideshow' with {
  attributes: attributes,
  uuid: paragraph.uuid(),
  title: paragraph.field_slideshow_title.value,
  columns_mobile: paragraph.field_slideshow_columns_mobile.value,
  columns_tablet: paragraph.field_slideshow_columns_tablet.value,
  columns_desktop: paragraph.field_slideshow_columns_desktop.value,
} %}

  {% block content %}
    {#
    The 'content' variable here contains the rendered output of the
    'field_slideshow_slides' field, which will be a series of <li> elements
    generated by our `paragraph--kingly-paragraph-slideshow-slide.html.twig`
    template.
    #}
    {{- content -}}
  {% endblock %}

{% endembed %}
